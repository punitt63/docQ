management:
  metrics:
    distribution:
      percentiles-histogram:
        http:
          server:
            requests: true
      percentiles:
        http:
          server:
            requests: 0.5,0.95,0.99,0.999
  endpoints:
    prometheus:
      enabled: true
    web:
      base-path: /
      exposure:
        include: health,prometheus
      path-mapping:
        health: '/health'

spring:
  application:
    name: health-facility
  jmx:
    enabled: false
  data:
    jpa:
      repositories:
        bootstrap-mode: deferred
  main:
    allow-bean-definition-overriding: true
  mvc:
    converters:
      preferred-json-mapper: gson
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    url: jdbc:postgresql://localhost:5432/health_facility
    username: docq
    password: docq
    driver: 'org.postgresql.Driver'
    poolSize: 20
    timeout: 2000
    hikari:
      pool-name: Hikari
      connection-timeout: 2000
      maximum-pool-size: 20
      auto-commit: false
      connection-test-query: select 1
      idle-timeout: 90000


  output:
    ansi:
      enabled: ALWAYS
  server:
    port: 9097
  servlet:
    session:
      cookie:
        http-only: true
    context-path: /

abha:
  client:
    id: test-abha-client-id
    secret: test-secret
  base:
    url: http://localhost:8080/api/

keycloak:
  realm: health-facility
  base:
    url: http://localhost:8080
  backend:
    client:
      id: health-facility-backend-app
      secret: PchdBBKN30i865FNVHOiMrEDpMbsNnes
  desktop:
    client:
      id: health-facility-desktop-app
      secret: 4kr76QXKae7U3sgXG4Q74brzqEnsFoRe
  http:
    max:
      idle:
        connections: 20
    keep:
      alive:
        duration:
          minutes: 60
    timeout:
      seconds: 20

encryption:
  aes:
    key: vHEyVZcMpQZx+7O2bQ0eUwqTY1AvXg8Whq2mDJK6ClU=
x:
  cm:
    id: sbx